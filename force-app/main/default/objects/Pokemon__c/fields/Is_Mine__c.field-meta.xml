<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Is_Mine__c</fullName>
    <description>Checkbox formula, true when the running user owns this Pokemon's trainer. Powers the My Pokemon list view — Pokemon is the detail side of a master-detail (no OwnerId), so the standard My scope is unavailable.</description>
    <label>Is Mine</label>
    <formula>Pokemon_Trainer__r.OwnerId = $User.Id</formula>
    <required>false</required>
    <type>Checkbox</type>
    <unique>false</unique>
</CustomField>
